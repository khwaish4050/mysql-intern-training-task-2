# mysql-intern-training-task-2
## Constraints Used and Reason

- PRIMARY KEY (id): Ensures each student has a unique identity
- NOT NULL (name, email, age): Prevents missing important data
- UNIQUE (email): Prevents duplicate student records
- AUTO_INCREMENT: Automatically generates student IDs
